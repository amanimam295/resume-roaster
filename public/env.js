// Optional per-deploy override, loaded before config.js.
// Some hosts (e.g. Render Static Sites) generate this file at build time:
//   echo "window.API_BASE_URL_OVERRIDE='https://your-api.onrender.com';" > public/env.js
// Leave as-is for local/same-origin development.
